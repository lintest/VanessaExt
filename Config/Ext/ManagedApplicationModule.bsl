Процедура ПриНачалеРаботыСистемы()
	
	Попытка
		ИмяФормы = AppServer.ПодключитьОбработку(ПараметрЗапуска);
		ПараметрыФормы = Новый Структура("Автотестирование", Истина);
		ПолучитьФорму(ИмяФормы, ПараметрыФормы).Открыть();
	Исключение
		ЗавершитьРаботуСистемы(Ложь);
	КонецПопытки;
	
КонецПроцедуры