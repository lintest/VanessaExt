{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build 1cWinCtrl64",
            "type": "shell",
            "command": "${workspaceRoot}/build.sh",
            "args": [ "64" ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Build Test",
            "type": "shell",
            "command": "make",
        }
    ]
}